# Prototypes

Local experiments — not deployed.

## Halftone hero preview

Open **`hero-halftone.html`** in your browser (double-click or drag into Chrome).

Assets load from `../brand_assets/hero/`. If the image is missing, run `node scripts/export-hero-assets.mjs` from the project root.
